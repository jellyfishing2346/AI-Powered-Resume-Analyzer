## Dockerfile moved to `deployment/Dockerfile` to keep repository root clean.
## Use `deployment/Dockerfile` for building production images.
## Example: docker build -f deployment/Dockerfile -t resume-analyzer:latest .